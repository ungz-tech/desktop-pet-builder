# 治愈系桌面宠物 · 可分享 Skill

让 AI 帮你制作自己的桌面伙伴：挑选喜欢的角色，赋予性格、表情、动作和彼此互动，再加入温柔的休息提醒。角色与称呼都可以自定义。

## 里面有什么

- `desktop-pet-builder/SKILL.md`：AI 使用的制作流程。
- `references/`：角色图集、自然动作、性格互动、使用时长变化、Mac 实现和验证方法。
- `scripts/scaffold.py`：生成独立项目，拒绝覆盖旧目录。
- `assets/macos-starter/`：能构建运行的 Mac 原生基础模板，包含窗口、点击、拖动、找回、大小、提醒和保存。

基础模板缺少角色 PNG 时会显示明确标记的占位图；AI 需根据你的要求制作/接入形象，并继续实现精细动作与剧情。分享包不附带原用户的角色图片、私人记录或成品应用。

## 安装

最省事：解压后，把完整 `desktop-pet-builder` 文件夹交给 Codex，并说：

> 请检查并安装这个 desktop-pet-builder skill，保留所有 references、scripts 和 assets 文件。

手动安装：将这个完整文件夹放入 `~/.agents/skills/`，让最终入口为 `~/.agents/skills/desktop-pet-builder/SKILL.md`。只想用于某个项目时，可以放入该项目的 `.agents/skills/`。不要只复制 SKILL.md。

Codex 会发现本地技能；如未出现，重启后查看。安装位置和调用方式参考 [OpenAI 官方技能说明](https://learn.chatgpt.com/docs/build-skills)。不同宿主的选择器可能显示为 `$` 或 `@`。

## 直接复制这段开始

> 请使用 $desktop-pet-builder，给我做一个 Mac 桌面宠物组合：一只橘猫和一只小狗。小小的、治愈可爱；点击会撒娇，它们会互相玩耍、依偎和打盹，每小时温柔地提醒我休息。角色外形和称呼由我来选，请完成到能在桌面运行。

也可以换成：

> 请使用 $desktop-pet-builder，制作两位我喜欢的 Q 版角色，称呼我“小星”。一个慢热稳重，一个热情贴心。增加专属互动，并让状态随着我连续使用电脑的时间变化。

已有项目：

> 请使用 $desktop-pet-builder，在这个桌面宠物项目里增加互相理毛与一起伸懒腰。保留原来的形象、大小、位置、亲近度和提醒设置。

## 需要什么

制作 Mac 应用需要能访问本机文件和执行代码的 AI 环境、macOS 与 Apple Command Line Tools。新建角色图片需要图像生成工具或你提供的透明 PNG。模板构建只用 Python 标准库和系统 Swift/AppKit，不要求语言模型 API 密钥。

本包已在 Mac 验证；Windows/Linux 用户可以让 AI 复用设计方法并适配窗口实现，包内没有这两个平台的运行模板。

## 分享一句话

> 分享一个我整理的“治愈系桌面宠物”Skill：让 AI 帮你做有性格、会互动的桌面伙伴。可以自选角色、称呼与陪伴方式，附制作流程、Mac 基础源码模板和测试方法。

直接转发 ZIP 即可。安装 skill 后还需要让 AI 按需求制作应用；它不会在安装时自动启动桌面宠物。

## 使用许可

本项目仅按 **PolyForm Noncommercial 1.0.0** 授权。非商业使用、修改和分享须遵守许可并保留署名；商用需另行取得授权。详见 [授权说明](授权说明.md)、[LICENSE](LICENSE) 和 [NOTICE](NOTICE)。
